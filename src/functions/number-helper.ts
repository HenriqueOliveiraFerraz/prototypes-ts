// export function getNumberGroup(num: string): number {
//   let zeros = '';

//   num.split('').forEach((f, i) => {
//     if (i > 0) {
//       zeros = zeros.concat('0');
//     }
//   });

//   return Number(`${num.charAt(0)}${zeros}`);
// }
